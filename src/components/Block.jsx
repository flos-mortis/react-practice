import React from "react"


const defaultCurrencies = ["RUB", "USD"];

export const Block = ({ value, currency, onChangeValue, onChangeCurrency }) => (
    <div className="block">
      <p>koko</p>
    </div>
  );